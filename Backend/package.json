{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend for the application",
  "main": "src/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "prisma generate && esbuild ./src --bundle --platform=node --target=node18 --outfile=dist/index.js --format=cjs && cd dist && cp -r ../node_modules/.prisma . && zip -r index.zip index.js .prisma",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate deploy"
  },
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.525.0",
    "@prisma/client": "^6.6.0",
    "pg": "^8.11.3",
    "firebase-admin": "^12.0.0",
    "jsonwebtoken": "^9.0.2",
    "@types/jsonwebtoken": "^9.0.5"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.130",
    "@types/pg": "^8.10.9",
    "esbuild": "^0.25.2",
    "prisma": "^6.6.0",
    "typescript": "^5.0.0"
  }
}
