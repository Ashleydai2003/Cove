import SwiftUI

struct Tmp: View {
    var body: some View {
        Text("app here")
            .font(.LibreBodoni(size: 35))
            .foregroundColor(Colors.primaryDark)
    }
}

#Preview {
    Tmp()
}
